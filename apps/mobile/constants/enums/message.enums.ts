/**
 * Message-related enumerations
 */

export enum MESSAGE_TYPE {
  TEXT = 'text',
  IMAGE = 'image',
  DOCUMENT = 'document',
  LOCATION = 'location',
}

export enum CHAT_SENDER {
  CUSTOMER = 'customer',
  RIDER = 'rider',
  SYSTEM = 'system',
}
