/**
 * User-related enumerations
 */

export enum USER_TYPE {
  CUSTOMER = 'customer',
  RIDER = 'rider',
}

export enum USER_STATUS {
  ACTIVE = 'active',
  INACTIVE = 'inactive',
  SUSPENDED = 'suspended',
}
